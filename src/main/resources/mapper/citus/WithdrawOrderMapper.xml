<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.web3.txsentry.mapper.citus.WithdrawOrderMapper">

    <insert id="insertOrder" parameterType="com.web3.txsentry.entity.WithdrawOrder" useGeneratedKeys="true" keyProperty="id">
        insert into withdraw_order (biz_order_id, to_address, token_address, amount, status, create_time, update_time)
        values (#{bizOrderId}, #{toAddress}, #{tokenAddress}, #{amount}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateStatusAndTxHash">
        update withdraw_order
        set status = #{status},
            tx_hash = #{txHash},
            update_time = NOW()
        where biz_order_id = #{bizOrderId}
    </update>

    <select id="selectByBizOrderId" resultType="com.web3.txsentry.entity.WithdrawOrder">
        select id,
               biz_order_id as bizOrderId,
               to_address as toAddress,
               amount,
               status,
               tx_hash as txHash,
               nonce,
               create_time as createTime,
               update_time as updateTime
        from withdraw_order
        where biz_order_id = #{bizOrderId}
    </select>
    <select id="selectStuckOrders" resultType="com.web3.txsentry.entity.WithdrawOrder">
        SELECT id, biz_order_id as bizOrderId, to_address as toAddress, token_address as tokenAddress,
               amount, status, tx_hash as txHash, nonce, create_time as createTime, update_time as updateTime
        FROM withdraw_order
        WHERE status = #{status}
          AND update_time &lt; #{thresholdTime}
    </select>

    <update id="updateTxHashForSpeedUp">
        UPDATE withdraw_order
        SET tx_hash = #{newTxHash},
            update_time = NOW()
        WHERE biz_order_id = #{bizOrderId}
    </update>
    <select id="selectByStatus" resultType="com.web3.txsentry.entity.WithdrawOrder">
        select id,
               biz_order_id as bizOrderId,
               to_address as toAddress,
               amount,
               status,
               tx_hash as txHash,
               nonce,
               create_time as createTime,
               update_time as updateTime
        from withdraw_order
        where status = #{status}
    </select>
</mapper>